# TextGuard 部署操作指南

> 本文档覆盖：首次部署、日常更新、紧急回滚三种场景。
> 所有操作均基于「本地构建 + 服务器运行」模式，服务器无需联网下载依赖。

---

## 服务器信息

| 项目 | 值 |
|------|-----|
| 服务器地址 | 按实际填写 |
| 部署目录 | `/opt/TextGuard/` |
| 上传文件目录 | `/opt/UploadFile/textGuardUpload/` |
| 访问地址 | http://服务器IP:3022（默认HTTP模式） |
| HTTPS 地址 | https://your-domain:3022（可选，需配置SSL证书） |

---

## 一、首次部署（全新服务器）

### 本地操作

1. **确认 Docker Desktop 已启动**（任务栏鲸鱼图标）

2. **双击 `build_images.bat`**，等待构建完成，生成：
   ```
   docker-images\textguard-backend.tar   (~200MB)
   docker-images\textguard-frontend.tar  (~25MB)
   ```

### 上传到服务器

使用 SFTP 工具（WinSCP / Xftp / 宝塔面板），将文件上传到 `/opt/TextGuard/`：

```
/opt/TextGuard/                    ← 服务器目录
├── docker-compose.yml             ← 容器编排配置
├── deploy.sh                      ← 部署脚本
├── backend/
│   └── .env.production            ← 后端环境变量配置
└── docker-images/
    ├── textguard-backend.tar      ← 后端镜像
    └── textguard-frontend.tar     ← 前端镜像
```

**上传文件清单（共5个）：**

| 序号 | 本地路径 | 服务器路径 |
|------|---------|-----------|
| 1 | `docker-compose.yml` | `/opt/TextGuard/docker-compose.yml` |
| 2 | `deploy.sh` | `/opt/TextGuard/deploy.sh` |
| 3 | `backend\.env.production` | `/opt/TextGuard/backend/.env.production` |
| 4 | `docker-images\textguard-backend.tar` | `/opt/TextGuard/docker-images/textguard-backend.tar` |
| 5 | `docker-images\textguard-frontend.tar` | `/opt/TextGuard/docker-images/textguard-frontend.tar` |

> 服务器目录不存在时，手动创建：`mkdir -p /opt/TextGuard/docker-images /opt/TextGuard/backend`

### 服务器执行

```bash
cd /opt/TextGuard
bash deploy.sh
```

脚本自动完成：创建目录 → 加载镜像 → 启动3个容器 → 显示运行状态。

等待约 15 秒后看到 `✅ 部署完成！` 即表示成功。

### 验证

```bash
# 查看容器状态（应有3个 running）
docker compose ps

# 测试后端健康检查
curl http://localhost:3020/api/v1/health

# 测试前端访问
curl -k https://localhost:3022
```

---

## 二、日常更新

### 场景 A：只改了后端代码（.py 文件）

**无需重新构建镜像**，直接上传变更文件 + 重启：

1. 上传修改的 `.py` 文件到服务器对应路径

   > 注意：由于容器中代码是镜像内置的，**直接上传文件不会生效**。
   > 此场景需要重新构建后端镜像（见场景 C）。

### 场景 B：只改了前端代码（.vue/.ts 文件）

同上，前端代码编译在镜像中，**需要重新构建前端镜像**（见场景 D）。

### 场景 C：更新后端（重新构建后端镜像）

**本地操作：**
1. 双击 `build_images.bat`（会重新构建所有镜像）
2. 只上传 `docker-images\textguard-backend.tar` 到服务器

**服务器操作：**
```bash
cd /opt/TextGuard
docker load -i docker-images/textguard-backend.tar
docker compose up -d backend celery-worker
```

> 只重启后端和celery，前端不受影响，用户无感知。

### 场景 D：更新前端

**本地操作：**
1. 双击 `build_images.bat`
2. 只上传 `docker-images\textguard-frontend.tar` 到服务器

**服务器操作：**
```bash
cd /opt/TextGuard
docker load -i docker-images/textguard-frontend.tar
docker compose up -d frontend
```

### 场景 E：全量更新（前后端都改了）

**本地操作：**
1. 双击 `build_images.bat`
2. 上传两个 `.tar` 文件到服务器

**服务器操作：**
```bash
cd /opt/TextGuard
bash deploy.sh
```

### 场景 F：只改了配置文件（.env.production）

1. 上传新的 `backend/.env.production` 到服务器
2. 服务器重启：
```bash
cd /opt/TextGuard
docker compose restart backend celery-worker
```

### 场景 G：只改了 docker-compose.yml

1. 上传新的 `docker-compose.yml` 到服务器
2. 服务器重新启动：
```bash
cd /opt/TextGuard
docker compose up -d
```

---

## 三、常用运维命令

```bash
# 查看所有容器状态
docker compose ps

# 查看实时日志（全部）
docker compose logs -f

# 查看后端日志
docker compose logs -f backend

# 查看 celery 日志
docker compose logs -f celery-worker

# 重启全部
docker compose restart

# 重启单个
docker compose restart backend

# 停止全部
docker compose down

# 查看镜像列表
docker images | grep textguard

# 清理旧镜像（释放磁盘）
docker image prune -f
```

---

## 四、紧急回滚

如果新版本有问题，需要回退到上一个版本：

1. 将上一次的 `.tar` 镜像文件重新上传（如果服务器上还保留着旧的就不用）
2. 加载旧镜像：
```bash
docker load -i docker-images/textguard-backend.tar
docker load -i docker-images/textguard-frontend.tar
```
3. 重启：
```bash
docker compose up -d
```

> 建议：每次部署前在服务器保留上一版镜像备份：
> ```bash
> mv docker-images/textguard-backend.tar docker-images/textguard-backend.tar.bak
> ```

---

## 五、注意事项

1. **deploy.sh 会自动创建所需目录**，无需手动 mkdir
2. **服务器不需要源代码**，只需要上述 5 个文件
3. **数据库迁移**在本地执行（本地和服务器共用同一数据库）：
   ```bash
   cd backend
   venv\Scripts\alembic upgrade head
   ```
4. **SSL 证书**已打包在前端镜像中，无需单独上传
5. 构建后的 `.tar` 文件建议本地保留一份，方便紧急回滚
6. **deploy.sh 部署完成后会询问是否删除 tar 文件**，如果磁盘空间充足建议保留
