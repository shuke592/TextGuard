# TextGuard 数据库初始化说明

## 一、数据库准备

### 1. 安装 PostgreSQL

**Windows:**
- 下载：https://www.postgresql.org/download/windows/
- 安装后默认端口：5432
- 记住设置的 postgres 用户密码

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install postgresql postgresql-contrib
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

**macOS:**
```bash
brew install postgresql@14
brew services start postgresql@14
```

### 2. 创建数据库

```bash
# 登录 PostgreSQL
psql -U postgres

# 创建数据库
CREATE DATABASE textguard;

# 创建用户（可选，建议生产环境使用）
CREATE USER textguard_user WITH PASSWORD 'your-password';
GRANT ALL PRIVILEGES ON DATABASE textguard TO textguard_user;

# 退出
\q
```

### 3. 安装 Redis

**Windows:**
- 下载：https://github.com/tporadowski/redis/releases
- 解压后运行 `redis-server.exe`

**Linux (Ubuntu/Debian):**
```bash
sudo apt install redis-server
sudo systemctl start redis
sudo systemctl enable redis
```

**macOS:**
```bash
brew install redis
brew services start redis
```

---

## 二、配置后端环境

### 1. 修改 `.env` 文件

```bash
cd backend
cp .env.example .env
```

编辑 `.env` 文件，修改以下配置：

```ini
# PostgreSQL 数据库配置
DB_HOST=localhost
DB_PORT=5432
DB_NAME=textguard
DB_USER=postgres
DB_PASSWORD=你的数据库密码
DATABASE_URL=postgresql+asyncpg://postgres:你的数据库密码@localhost:5432/textguard

# Redis 配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# 大模型配置（至少配置一个）
DEEPSEEK_API_KEY=你的DeepSeek API Key
DEEPSEEK_API_BASE=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
```

### 2. 安装 Python 依赖

```bash
# 创建虚拟环境
python -m venv venv

# 激活虚拟环境
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

---

## 三、初始化数据库

### 方式一：使用初始化脚本（推荐）

```bash
cd backend

# 1. 创建数据库表结构
alembic upgrade head

# 2. 初始化测试数据（管理员账号、角色权限、示例配置）
python init_database.py
```

**初始化完成后，会自动创建：**

| 类型 | 内容 |
|------|------|
| **角色** | 超级管理员、普通用户、游客 |
| **权限** | 23 个权限节点（菜单 + 按钮级权限） |
| **用户** | admin、user001、guest（密码见下方） |
| **大模型配置** | DeepSeek、OpenAI、Kimi 示例配置 |

**默认账号：**

| 工号 | 密码 | 角色 | 说明 |
|------|------|------|------|
| `admin` | `admin123` | 超级管理员 | 拥有所有权限 |
| `user001` | `user123` | 普通用户 | 可使用核心功能 |
| `guest` | `guest123` | 游客 | 功能受限，有配额限制 |

### 方式二：使用 Alembic 迁移 + Seed 脚本

```bash
cd backend

# 1. 创建数据库表结构
alembic upgrade head

# 2. 运行种子数据脚本
python -c "from app.core.seed import run_seed; import asyncio; asyncio.run(run_seed())"
```

---

## 四、验证初始化

### 1. 启动后端服务

```bash
cd backend
uvicorn app.main:app --host 0.0.0.0 --port 3020 --reload
```

### 2. 访问 API 文档

打开浏览器访问：http://localhost:3020/docs

### 3. 测试登录

**方法 A：通过 API 文档测试**

1. 访问 http://localhost:3020/docs
2. 找到 `POST /api/v1/auth/login` 接口
3. 点击 `Try it out`
4. 输入：
   ```json
   {
     "employee_id": "admin",
     "password": "admin123"
   }
   ```
5. 点击 `Execute`，应返回 `access_token`

**方法 B：使用 curl 测试**

```bash
curl -X POST "http://localhost:3020/api/v1/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"employee_id":"admin","password":"admin123"}'
```

### 4. 启动前端并登录

```bash
# 新开一个终端
cd frontend
npm run dev
```

访问 http://localhost:3022，使用 `admin` / `admin123` 登录。

---

## 五、常见问题

### Q1: 数据库连接失败

**错误信息：**
```
sqlalchemy.exc.OperationalError: could not connect to server
```

**解决方法：**
1. 确认 PostgreSQL 服务已启动
2. 检查 `.env` 中的数据库配置是否正确
3. 测试连接：
   ```bash
   psql -h localhost -U postgres -d textguard
   ```

### Q2: Redis 连接失败

**错误信息：**
```
redis.exceptions.ConnectionError: Error connecting to Redis
```

**解决方法：**
1. 确认 Redis 服务已启动
2. 测试连接：
   ```bash
   redis-cli ping
   # 应返回 PONG
   ```

### Q3: 初始化脚本报错 "表已存在"

**解决方法：**

如果需要重新初始化，先清空数据库：

```bash
# 登录数据库
psql -U postgres -d textguard

# 删除所有表
DROP SCHEMA public CASCADE;
CREATE SCHEMA public;
GRANT ALL ON SCHEMA public TO postgres;
GRANT ALL ON SCHEMA public TO public;

# 退出
\q

# 重新初始化
cd backend
alembic upgrade head
python init_database.py
```

### Q4: 大模型配置无法使用

**原因：**
初始化脚本创建的大模型配置中，API Key 是占位符，需要手动配置。

**解决方法：**
1. 登录管理后台：http://localhost:3022
2. 进入 `管理后台` → `大模型配置`
3. 编辑配置，填入真实的 API Key
4. 点击 `激活` 按钮

### Q5: 忘记管理员密码

**解决方法：**

```bash
cd backend
python -c "
from app.core.security import hash_password
print(hash_password('new_password'))
"
# 复制输出的哈希值

# 登录数据库
psql -U postgres -d textguard

# 更新密码
UPDATE users SET password_hash='粘贴上面的哈希值' WHERE employee_id='admin';
```

---

## 六、生产环境部署

生产环境建议：

1. **修改默认密码**
   - 登录后立即修改 admin 账号密码
   - 删除或禁用测试账号（user001、guest）

2. **配置真实的大模型 API Key**
   - 在管理后台配置真实的 API Key
   - 删除示例配置

3. **使用独立的数据库用户**
   ```sql
   CREATE USER textguard_user WITH PASSWORD 'strong-password';
   GRANT ALL PRIVILEGES ON DATABASE textguard TO textguard_user;
   ```

4. **启用 Redis 密码**
   ```bash
   # 编辑 redis.conf
   requirepass your-redis-password
   ```

5. **备份数据库**
   ```bash
   pg_dump -U postgres textguard > textguard_backup.sql
   ```

---

## 七、数据库表结构说明

初始化后会创建以下表：

| 表名 | 说明 |
|------|------|
| `users` | 用户表 |
| `roles` | 角色表 |
| `permissions` | 权限表 |
| `role_permissions` | 角色-权限关联表 |
| `llm_configs` | 大模型配置表 |
| `global_words` | 全局词库表 |
| `dictionaries` | 个人词库表 |
| `proofread_records` | 校对记录表 |
| `uploaded_documents` | 上传文档表 |
| `audit_logs` | 审计日志表 |
| `alembic_version` | 数据库版本表 |

---

## 八、下一步

初始化完成后，建议：

1. 阅读 [用户使用手册](用户使用手册.md) 了解功能使用
2. 阅读 [系统运维操作手册](系统运维操作手册.md) 了解运维操作
3. 阅读 [部署操作指南](部署操作指南.md) 了解生产部署

如有问题，请在 GitHub/Gitee 提交 Issue。
