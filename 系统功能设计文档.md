# TextGuard 系统功能设计文档

> 版本：v1.0 | 更新日期：2026-05-14

---

## 一、技术栈

| 层级 | 技术选型 |
|------|----------|
| 后端框架 | Python 3.10+ / FastAPI + Uvicorn |
| 数据库 | PostgreSQL（异步 SQLAlchemy + Alembic 迁移） |
| 缓存/消息 | Redis |
| 异步任务 | Celery + Redis |
| 认证 | JWT（access_token 7天 / refresh_token 30天） |
| 大模型 | 多供应商（DeepSeek / OpenAI / 通义千问 / 智谱AI / Kimi / LiteLLM / Azure / 自定义兼容） |
| 文档解析 | python-docx、PyMuPDF |
| 日志 | Loguru |
| 前端框架 | Vue 3 + TypeScript + Vite |
| UI 库 | Element Plus |
| 状态管理 | Pinia |
| 文本编辑器 | Tiptap |
| 文档预览 | docx-preview、pdf.js |

---

## 二、项目结构

```
TextGuard/
├── backend/                    # 后端 FastAPI
│   ├── app/
│   │   ├── api/v1/             # REST API 路由
│   │   │   ├── auth.py         # 认证（登录/注册/个人中心）
│   │   │   ├── feishu.py       # 飞书认证（扫码/SSO/回调）
│   │   │   ├── proofread.py    # 文本校对
│   │   │   ├── document.py     # 文档上传校对
│   │   │   ├── polish.py       # AI润色
│   │   │   ├── dictionary.py   # 个性化词库
│   │   │   ├── whitelist.py    # 放行词管理
│   │   │   ├── history.py      # 校对历史
│   │   │   ├── tasks.py        # 异步任务状态
│   │   │   ├── site_info.py    # 站点配置（公开）
│   │   │   └── admin/          # 管理后台API
│   │   │       ├── users.py        # 用户管理
│   │   │       ├── roles.py        # 角色权限
│   │   │       ├── llm_config.py   # 大模型配置
│   │   │       ├── global_dict.py  # 全局词库
│   │   │       ├── audit.py        # 审计日志
│   │   │       └── settings.py     # 系统设置
│   │   ├── core/               # 核心配置
│   │   │   ├── config.py       # 环境变量配置（Pydantic Settings）
│   │   │   ├── database.py     # 数据库连接池
│   │   │   ├── redis.py        # Redis 客户端
│   │   │   ├── security.py     # JWT / 密码哈希
│   │   │   └── dependencies.py # 公共依赖（权限校验等）
│   │   ├── models/             # SQLAlchemy 数据模型
│   │   ├── schemas/            # Pydantic 请求/响应
│   │   ├── services/           # 业务逻辑
│   │   │   ├── feishu.py       # 飞书OAuth服务
│   │   │   ├── proofread.py    # 校对服务（Prompt构建/分片/调用）
│   │   │   ├── document.py     # 文档解析服务
│   │   │   ├── polish.py       # AI润色服务
│   │   │   ├── llm/            # 大模型抽象层
│   │   │   └── audit_log.py    # 审计日志服务
│   │   └── tasks/              # Celery 异步任务
│   ├── alembic/                # 数据库迁移
│   ├── Dockerfile
│   └── requirements.txt
├── frontend/                   # 前端 Vue 3
│   ├── src/
│   │   ├── api/                # API 请求封装
│   │   ├── layouts/            # 布局（UserLayout / AdminLayout）
│   │   ├── router/             # 路由 + 权限守卫
│   │   ├── stores/             # Pinia 状态管理
│   │   ├── views/              # 页面视图
│   │   └── directives/         # 自定义指令（权限控制）
│   ├── ssl/                    # SSL 证书
│   ├── Dockerfile
│   └── nginx.production.conf
├── docker-compose.yml          # Docker 编排
└── deploy.sh                   # 一键部署脚本
```

---

## 三、数据模型

### 3.1 核心表

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `users` | 用户 | employee_id, username, phone, role_id, feishu_open_id, feishu_union_id, is_active |
| `roles` | 角色 | code(super_admin/admin/user), permissions(JSON) |
| `proofread_records` | 校对记录 | user_id, type(text/document/polish), original_text, result_json |
| `dictionaries` | 用户词库 | user_id, name, is_active |
| `dictionary_entries` | 词条 | dictionary_id, wrong_word, correct_word |
| `whitelist_words` | 放行词 | user_id, word, type(permanent/temporary), expires_at |
| `global_words` | 全局词库 | word, replacement, category(sensitive/banned/correction/whitelist), severity |
| `llm_configs` | 大模型配置 | provider, api_base, api_key, model_name, is_active |
| `audit_logs` | 审计日志 | action_type, user_id, client_ip, input_text, status, duration_ms |

### 3.2 飞书关联字段（users 表）

| 字段 | 说明 |
|------|------|
| feishu_open_id | 飞书应用内用户ID |
| feishu_union_id | 飞书企业内唯一ID（unique） |
| feishu_user_id | 飞书用户ID |
| last_login_at | 最后登录时间 |
| login_method | 登录方式：password / feishu |

---

## 四、API 设计

所有接口统一前缀 `/api/v1/`

### 4.1 认证

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /auth/login | 工号密码登录 |
| POST | /auth/refresh | 刷新 Token |
| GET | /auth/me | 获取当前用户 |
| PUT | /auth/profile | 修改个人信息 |
| PUT | /auth/password | 修改密码 |
| POST | /auth/avatar | 上传头像 |
| GET | /auth/feishu/config | 飞书扫码配置 |
| POST | /auth/feishu/callback | 飞书授权回调 |
| GET | /auth/feishu/sso | 飞书工作台SSO |

### 4.2 业务功能

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /proofread/text | 文本校对 |
| GET | /polish/styles | 润色风格列表 |
| POST | /polish/text | AI润色 |
| POST | /document/upload | 文档上传 |
| POST | /document/{id}/proofread | 文档校对 |
| GET | /document/{id}/download-revised | 下载修订文档 |
| GET/POST/PUT/DELETE | /dictionary/* | 个性化词库 CRUD |
| GET/POST/PUT/DELETE | /whitelist/* | 放行词 CRUD |
| GET | /history | 校对历史列表 |
| GET | /history/{id} | 校对历史详情 |

### 4.3 管理后台

| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST/PUT/DELETE | /admin/users/* | 用户管理 |
| POST | /admin/users/{id}/reset-password | 重置密码 |
| PUT | /admin/users/{id}/toggle-active | 停用/启用 |
| GET/POST/PUT/DELETE | /admin/roles/* | 角色权限 |
| GET/POST/PUT/DELETE | /admin/llm-configs/* | 大模型配置 |
| POST | /admin/llm-configs/{id}/test | 测试连接 |
| GET/POST/PUT/DELETE | /admin/global-dict/* | 全局词库 |
| GET | /admin/audit-logs | 审计日志 |
| GET/PUT | /admin/settings/site | 站点配置 |

---

## 五、核心业务逻辑

### 5.1 校对流程

```
用户输入文本 → 长文本自动分片(~2000字/片) → 构建 System Prompt
  ├─ 基础指令（角色/输出JSON格式）
  ├─ 领域规则（通用/公文/法律/电力/新能源/电能表）
  └─ 全局词库注入（敏感词/禁词/纠错/放行词采样）
→ 并发调用大模型（Semaphore 限4并发）→ 合并结果 → 返回问题列表
```

### 5.2 大模型抽象层

统一 `OpenAICompatProvider` 兼容所有供应商，支持：
- Endpoint 智能排序（记忆首次成功路径）
- 超时 180s / 重试 2 次
- 管理后台动态切换活跃模型

### 5.3 飞书登录流程

详见 `飞书对接流程及配置及开发文档.md`

### 5.4 RBAC 权限体系

- 三级角色：`super_admin` > `admin` > `user`
- 权限粒度：页面级 + 按钮级（前端 `v-permission` 指令）
- 权限存储：角色 `permissions` JSON 数组

---

## 六、开发规范

- 后端分层：API(路由) → Service(业务逻辑) → Model(数据模型)
- 前端风格：Vue 3 Composition API + `<script setup>`
- API 前缀：`/api/v1/`
- UI 风格：企业级稳重设计，主题色 `#0056b3`，背景色 `#F5F5F5`
- 异步关系加载：`selectinload` 避免 async greenlet 错误
- 数据库连接池：`pool_recycle=3600`, `pool_pre_ping=True`
- Redis 异常不阻塞主流程（降级处理）

---

## 七、生产架构

```
用户浏览器 → http(s)://your-domain:3022
                  ↓
        textguard-frontend (Nginx + 前端 + 反代, 可选SSL)
                  ↓ /api/*
        textguard-backend  (FastAPI, network_mode: host)
                  ↓
        textguard-celery   (Celery Worker, network_mode: host)
                  ↓
        PostgreSQL / Redis (外部服务，非容器化)
```

backend 和 celery 使用 `network_mode: host` 直接走宿主机网络，解决 Docker bridge DNS 不稳定问题。
