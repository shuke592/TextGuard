# TextGuard 系统运维操作手册

> 版本：v1.0 | 更新日期：2026-05-14

---

## 一、环境要求

| 项目 | 要求 |
|------|------|
| 服务器 | Ubuntu 22.04+，已安装 Docker |
| Python | 3.10+（本地开发） |
| Node.js | 18+（本地开发） |
| PostgreSQL | 14+（本地或远程） |
| Redis | 7+（本地或远程） |
| 域名 | your-domain.com → 服务器 IP（可选） |

---

## 二、本地开发

### 2.1 首次安装

```bash
# 双击运行根目录
安装依赖.bat
```

手动安装：

```bash
# 后端
cd backend
python -m venv venv
venv\Scripts\pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/
venv\Scripts\alembic upgrade head    # 数据库迁移

# 前端
cd frontend
npm install --registry=https://registry.npmmirror.com
```

### 2.2 日常启动

```bash
# 一键启动（推荐，自动清理端口+启动前后端）
start_dev.bat

# 或分别启动
cd backend && start_backend.bat
cd frontend && start_frontend.bat
```

### 2.3 访问地址

| 服务 | 地址 |
|------|------|
| 前端 | http://localhost:3022 |
| 后端 API 文档 | http://localhost:3020/docs |
| 健康检查 | http://localhost:3020/api/v1/health |
| 默认账号 | admin / admin123 |

### 2.4 环境变量

编辑 `backend/.env`：

| 配置项 | 说明 |
|--------|------|
| DATABASE_URL | PostgreSQL 连接地址 |
| REDIS_HOST / REDIS_PASSWORD | Redis 连接 |
| DEEPSEEK_API_KEY | DeepSeek API 密钥 |
| JWT_SECRET_KEY | JWT 密钥（生产环境必改） |
| FEISHU_APP_ID / FEISHU_APP_SECRET | 飞书应用凭证 |
| FEISHU_REDIRECT_URI | 飞书回调地址 |
| FEISHU_ENABLED | 是否启用飞书登录 |

---

## 三、生产环境部署

### 3.1 架构

3 个 Docker 容器 + 外部数据库：

| 容器 | 端口 | 说明 |
|------|------|------|
| textguard-frontend | 3022(HTTPS) / 3080(HTTP→跳转) | Nginx + SSL + 前端 + 反向代理 |
| textguard-backend | 3020(内部) | FastAPI（host 网络模式） |
| textguard-celery | — | Celery Worker（host 网络模式） |

### 3.2 一键部署

```bash
# 上传项目到服务器
scp -r TextGuard/ root@服务器IP:/opt/

# 登录服务器执行
cd /opt/TextGuard
chmod +x deploy.sh
./deploy.sh
```

### 3.3 手动部署

```bash
cd /opt/TextGuard
mkdir -p /opt/UploadFile/textGuardUpload
docker compose up -d --build
docker compose ps     # 验证状态
```

### 3.4 验证

```bash
curl -k https://localhost:3022/api/v1/health
curl -I http://localhost:3080   # 应 301 跳转 HTTPS
```

浏览器访问：`http://服务器IP:3022`（如配置HTTPS则使用 `https://your-domain:3022`）

### 3.5 生产配置文件

| 文件 | 说明 |
|------|------|
| `backend/.env.production` | 生产环境变量（含密钥，勿泄露） |
| `frontend/ssl/` | SSL 证书（可选，启用HTTPS时需要） |
| `frontend/nginx.production.conf` | Nginx 生产配置 |
| `docker-compose.yml` | 容器编排 |
| `deploy.sh` | 一键部署脚本 |

---

## 四、日常运维

### 4.1 常用命令

```bash
# 查看状态
docker compose ps

# 查看日志
docker compose logs -f              # 全部
docker compose logs -f backend      # 仅后端
docker compose logs --tail=100 backend  # 最近100行

# 重启
docker compose restart              # 全部
docker compose restart backend      # 仅后端

# 停止
docker compose down

# 更新代码后重新部署
docker compose up -d --build
```

### 4.2 数据备份

```bash
# 备份上传文件
tar -czf textguard_uploads_$(date +%Y%m%d).tar.gz /opt/UploadFile/textGuardUpload/

# 备份数据库
pg_dump -U postgres textguard > textguard_db_$(date +%Y%m%d).sql
```

### 4.3 SSL 证书续期

替换 `frontend/ssl/` 下的证书文件后：

```bash
docker compose up -d --build frontend
```

### 4.4 磁盘清理

```bash
docker system prune -f   # 清理无用镜像/容器
```

---

## 五、故障排查

### 5.1 容器启动失败

```bash
docker compose logs <容器名>
```

### 5.2 无法访问

```bash
ss -tlnp | grep -E '3022|3080'    # 端口检查
sudo ufw status                     # 防火墙
nslookup your-domain.com            # DNS
```

### 5.3 Nginx 配置问题

```bash
docker compose exec frontend nginx -t
docker compose exec frontend ls -la /etc/nginx/ssl/
```

### 5.4 后端启动失败（本地）

```powershell
# 检查端口占用
Get-NetTCPConnection -LocalPort 3020
# 检查依赖
cd backend && venv\Scripts\pip install -r requirements.txt
```

### 5.5 数据库迁移

```bash
cd backend
venv\Scripts\alembic upgrade head        # 本地执行即可（本地和生产共用数据库）
```

---

## 六、防火墙

```bash
sudo ufw allow 3022/tcp   # HTTPS
sudo ufw allow 3080/tcp   # HTTP 跳转
```

---

## 七、安全配置

- SSL/TLS 1.2+ 加密，HSTS 安全头
- HTTP 自动 301 跳转 HTTPS
- DEBUG=false（生产环境）
- 数据库/Redis 不暴露外部端口
- Docker 日志大小限制（防磁盘溢出）
- CORS 仅允许指定域名
- Celery 任务软/硬超时保护（5min/6min）

---

## 八、性能配置

| 项目 | 值 |
|------|-----|
| Uvicorn Workers | 2 |
| Celery Workers | 2（solo pool） |
| Redis 内存限制 | 256MB（LRU 淘汰） |
| 上传文件限制 | 50MB |
| Nginx Gzip | 已启用（级别 6） |
| 数据库连接池 | pool_recycle=3600, pool_pre_ping=True |

---

## 九、文件存储

| 环境 | 路径 |
|------|------|
| 本地开发 | `backend/uploads/` |
| 生产环境 | `/opt/UploadFile/textGuardUpload/`（Docker volume 挂载） |

包含：用户头像、上传文档、导出文件、平台图标。
