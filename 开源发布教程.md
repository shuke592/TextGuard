# TextGuard 开源发布完整教程

> 本教程面向首次开源项目的开发者，详细讲解如何将 TextGuard 发布到 GitHub 和 Gitee。

---

## 🎯 快速操作（仓库已创建，直接推送）

> 您的仓库已创建完毕，按以下步骤在 PowerShell 中逐步执行即可。

### 仓库信息

| 平台 | 地址 |
|------|------|
| GitHub | https://github.com/shuke592/TextGuard |
| Gitee | https://gitee.com/shuke592/TextGuard |

### 第一步：删除不应提交的文件

在 Windows 文件管理器中手动删除（如果存在）：

```
frontend/ssl/          ← 删除整个目录（SSL证书，含企业域名信息）
nginx/                 ← 删除整个目录（如果存在）
```

### 第二步：初始化 Git 并提交

打开 **PowerShell**，执行以下命令：

```powershell
# 进入项目目录
cd "C:\Users\wenbo\Desktop\开源\TextGuard"

# 初始化 Git 仓库
git init

# 添加所有文件（.gitignore 会自动排除敏感文件）
git add .

# 检查即将提交的文件（确认没有 .env、ssl 证书等敏感内容）
git status
```

**⚠️ 重要检查**：运行 `git status` 后，确认以下文件**没有**出现在列表中：
- `backend/.env`
- `backend/.env.production`
- `backend/.env.docker`
- `frontend/ssl/`
- `修改问题反馈记录.md`
- `飞书对接流程及配置及开发文档.md`
- `服务器IP封禁问题备忘.md`

如果有上述文件出现，说明 `.gitignore` 未生效，执行：
```powershell
git rm --cached backend/.env
git rm --cached backend/.env.production
git rm --cached backend/.env.docker
# 对其他文件类似操作
```

确认无误后，提交代码：

```powershell
git commit -m "feat: initial commit - TextGuard 智能文档审校平台开源版"
```

### 第三步：推送到 GitHub

```powershell
# 关联 GitHub 远程仓库
git remote add origin https://github.com/shuke592/TextGuard.git

# 设置主分支为 main
git branch -M main

# 推送代码到 GitHub
git push -u origin main
```

**认证方式**（GitHub 不再支持密码登录，需使用 Token）：

1. 打开浏览器访问：https://github.com/settings/tokens
2. 点击 `Generate new token` → `Generate new token (classic)`
3. **Note** 填写：`TextGuard push`
4. **Expiration** 选择：`90 days` 或 `No expiration`
5. **勾选权限**：✅ `repo`（第一个，包含所有子项）
6. 点击底部绿色 `Generate token` 按钮
7. **立即复制**生成的 Token（形如 `ghp_xxxxxxxxxxxx`，只显示一次！）

推送时系统弹出认证窗口：
- **Username**: `shuke592`
- **Password**: 粘贴刚才复制的 Token

### 第四步：推送到 Gitee

```powershell
# 添加 Gitee 作为第二个远程仓库
git remote add gitee https://gitee.com/shuke592/TextGuard.git

# 推送代码到 Gitee
git push -u gitee main
```

Gitee 认证方式：
- **Username**: 你的 Gitee 用户名或手机号
- **Password**: 你的 Gitee 密码

### 第五步：验证推送成功

打开浏览器分别访问：
- https://github.com/shuke592/TextGuard — 确认代码和 README 已显示
- https://gitee.com/shuke592/TextGuard — 确认代码和 README 已显示

### 第六步：配置仓库信息

#### GitHub 配置

1. 访问 https://github.com/shuke592/TextGuard
2. 点击仓库名称右侧的 **⚙️ 齿轮图标**（About 区域）
3. 填写：
   - **Description**: `🛡️ AI驱动的智能文档审校平台，支持AI润色、文本校对、文档上传校对，在线预览、问题高亮、一键/逐条修改，修改后导出。`
   - **Topics**: 逐个输入并回车确认：
     ```
     ai
     document-proofreading
     fastapi
     vue3
     nlp
     text-correction
     chinese-nlp
     docker
     deepseek
     ```
   - 勾选 ✅ **Releases**
   - 勾选 ✅ **Packages**（如果有）
4. 点击 **Save changes**

#### Gitee 配置

1. 访问 https://gitee.com/shuke592/TextGuard
2. 点击右上角 **管理**
3. 在「基本信息」中填写：
   - **仓库介绍**: `🛡️ AI驱动的智能文档审校平台，支持AI润色、文本校对、文档上传校对，在线预览、问题高亮、一键/逐条修改，修改后导出。`
   - **仓库标签**: 添加 `AI` `文档校对` `FastAPI` `Vue3` `NLP` `文本纠错` `Docker` `DeepSeek`
   - **开源许可证**: MIT License
   - **开发语言**: Python, TypeScript
4. 点击 **保存**

### 第七步：创建首个 Release（可选但推荐）

#### GitHub Release

1. 访问 https://github.com/shuke592/TextGuard/releases/new
2. 填写：
   - **Choose a tag**: 输入 `v1.0.0`，点击 `Create new tag: v1.0.0 on publish`
   - **Release title**: `v1.0.0 首个开源版本`
   - **Description** 填写：
     ```
     ## 🎉 TextGuard v1.0.0 正式开源！
     
     AI 驱动的智能文档审校平台，核心功能：
     
     - ✅ AI 智能润色（10种风格、三段式输出）
     - ✅ 文本在线校对（分片处理、高亮标注、逐条修改）
     - ✅ 文档上传校对（DOCX/PDF/TXT，保留排版）
     - ✅ 多大模型支持（DeepSeek/OpenAI/Kimi/通义千问等8种）
     - ✅ 专业词库系统（全局词库 + 个人词库 + 放行词）
     - ✅ 企业级管理（RBAC权限 + 审计日志）
     - ✅ Docker 一键部署
     
     ### 📦 快速开始
     
     git clone https://github.com/shuke592/TextGuard.git
     cd TextGuard
     详见 README.md
     ```
3. 点击 **Publish release**

---

## 🔧 后续维护：一键推送双平台

以后修改代码后，执行以下命令即可同步到两个平台：

```powershell
git add .
git commit -m "feat: 描述你的修改"
git push origin main
git push gitee main
```

或者使用一键推送脚本（双击 `push_all.bat`）。

---

## 📋 发布前检查清单（详细版）

在上传代码前，请确认以下事项已完成：

### ✅ 必须完成的清理工作

- [ ] **删除 SSL 证书目录**
  ```bash
  # 如果存在这些目录，请删除
  frontend/ssl/
  nginx/
  ```

- [ ] **确认敏感文件已排除**
  ```bash
  # 这些文件不应出现在仓库中（已在 .gitignore 中排除）
  backend/.env
  backend/.env.production
  backend/.env.docker
  修改问题反馈记录.md
  待修复问题汇总.md
  飞书对接流程及配置及开发文档.md
  服务器IP封禁问题备忘.md
  ```

- [x] **检查 README.md 中的占位符** ✅ 已完成
  - 仓库地址：`https://github.com/shuke592/TextGuard.git`

### ✅ 可选优化项

- [ ] 准备项目封面图（推荐尺寸：1280x640px）
- [ ] 准备功能截图（3-5张）
- [ ] 录制演示视频（可选）

---

## 🚀 方式一：发布到 GitHub

### 第一步：创建 GitHub 仓库

1. **登录 GitHub**
   - 访问 https://github.com
   - 登录你的账号

2. **创建新仓库**
   - 点击右上角 `+` → `New repository`
   - 填写仓库信息：

   | 字段 | 填写内容 |
   |------|---------|
   | **Repository name** | `TextGuard` |
   | **Description** | `🛡️ AI驱动的智能文档审校平台 - 开源、私有化部署、多模型支持` |
   | **Public/Private** | 选择 `Public`（公开） |
   | **Initialize repository** | ❌ 不要勾选任何选项（README、.gitignore、License 都不勾选） |

3. **点击 `Create repository`**

### 第二步：本地初始化 Git 仓库

打开 **PowerShell** 或 **Git Bash**，进入项目目录：

```bash
# 进入项目根目录
cd /path/to/TextGuard

# 初始化 Git 仓库
git init

# 添加所有文件（.gitignore 会自动排除敏感文件）
git add .

# 查看将要提交的文件（确认没有敏感信息）
git status

# 如果发现不应提交的文件，执行：
# git rm --cached <文件路径>

# 首次提交
git commit -m "feat: initial commit - TextGuard 智能文档审校平台"
```

### 第三步：关联远程仓库并推送

```bash
# 关联 GitHub 远程仓库（替换 shuke592 为你的 GitHub 用户名）
git remote add origin https://github.com/shuke592/TextGuard.git

# 推送到 GitHub（首次推送需要输入 GitHub 账号密码或 Token）
git branch -M main
git push -u origin main
```

**如果推送失败提示需要认证：**

#### 方法 A：使用 Personal Access Token（推荐）

1. 访问 https://github.com/settings/tokens
2. 点击 `Generate new token` → `Generate new token (classic)`
3. 勾选权限：`repo`（完整仓库权限）
4. 点击 `Generate token`，复制生成的 Token（只显示一次，务必保存）
5. 推送时输入：
   - Username: 你的 GitHub 用户名
   - Password: 粘贴刚才复制的 Token

#### 方法 B：使用 SSH 密钥

```bash
# 生成 SSH 密钥（如果已有可跳过）
ssh-keygen -t ed25519 -C "your-email@example.com"

# 复制公钥内容
cat ~/.ssh/id_ed25519.pub

# 访问 https://github.com/settings/keys
# 点击 New SSH key，粘贴公钥内容

# 修改远程仓库地址为 SSH 格式
git remote set-url origin git@github.com:shuke592/TextGuard.git

# 重新推送
git push -u origin main
```

### 第四步：完善仓库信息

推送成功后，访问你的仓库页面 `https://github.com/shuke592/TextGuard`

#### 1. 添加项目主题标签（Topics）

点击仓库页面右侧的 `⚙️ 设置图标` → `Add topics`，添加：

```
ai, document-proofreading, fastapi, vue3, nlp, text-correction, 
chinese-nlp, docker, deepseek, openai, document-editing
```

#### 2. 设置仓库描述

在仓库页面顶部点击 `About` 右侧的 `⚙️`，填写：

- **Description**: `🛡️ AI驱动的智能文档审校平台 - 开源、私有化部署、多模型支持`
- **Website**: 如果有演示站点可填写
- **Topics**: 已在上一步添加

#### 3. 上传项目封面图（可选）

在仓库根目录创建 `docs/images/` 文件夹，上传封面图：

```bash
mkdir -p docs/images
# 将封面图命名为 banner.png 放入该目录
git add docs/images/banner.png
git commit -m "docs: add project banner"
git push
```

然后在 `README.md` 顶部添加：

```markdown
<div align="center">
  <img src="docs/images/banner.png" alt="TextGuard Banner" width="100%">
</div>
```

#### 4. 创建 Release（发布版本）

1. 点击仓库页面右侧的 `Releases` → `Create a new release`
2. 填写信息：
   - **Tag version**: `v1.0.0`
   - **Release title**: `v1.0.0 - 首个开源版本`
   - **Description**: 
     ```markdown
     ## 🎉 TextGuard v1.0.0 正式开源！
     
     ### ✨ 核心功能
     - ✅ AI 智能润色（10种风格）
     - ✅ 文本在线校对
     - ✅ 文档上传校对（DOCX/PDF/TXT）
     - ✅ 多大模型支持（8种供应商）
     - ✅ 企业级权限管理
     - ✅ Docker 一键部署
     
     ### 📦 快速开始
     ```bash
     git clone https://github.com/shuke592/TextGuard.git
     cd TextGuard
     # 参考 README.md 进行部署
     ```
     
     ### 📖 文档
     - [快速开始](README.md#快速开始)
     - [Docker 部署](README.md#docker-部署)
     - [系统功能设计文档](系统功能设计文档.md)
     ```
3. 点击 `Publish release`

---

## 🇨🇳 方式二：发布到 Gitee（码云）

### 第一步：创建 Gitee 仓库

1. **登录 Gitee**
   - 访问 https://gitee.com
   - 登录你的账号

2. **创建新仓库**
   - 点击右上角 `+` → `新建仓库`
   - 填写仓库信息：

   | 字段 | 填写内容 |
   |------|---------|
   | **仓库名称** | `TextGuard` |
   | **仓库介绍** | `🛡️ AI驱动的智能文档审校平台 - 开源、私有化部署、多模型支持` |
   | **是否开源** | 选择 `开源` |
   | **开源许可证** | 选择 `MIT License` |
   | **初始化仓库** | ❌ 不勾选任何选项 |

3. **点击 `创建`**

### 第二步：推送代码到 Gitee

如果你已经推送到 GitHub，可以添加 Gitee 作为第二个远程仓库：

```bash
# 添加 Gitee 远程仓库（替换 shuke592 为你的 Gitee 用户名）
git remote add gitee https://gitee.com/shuke592/TextGuard.git

# 推送到 Gitee
git push -u gitee main
```

如果只发布到 Gitee（不发布 GitHub），执行：

```bash
cd /path/to/TextGuard
git init
git add .
git commit -m "feat: initial commit - TextGuard 智能文档审校平台"
git remote add origin https://gitee.com/shuke592/TextGuard.git
git branch -M main
git push -u origin main
```

### 第三步：完善 Gitee 仓库

#### 1. 设置仓库标签

在仓库页面点击 `管理` → `仓库设置` → `功能设置`，添加标签：

```
AI, 文档校对, FastAPI, Vue3, NLP, 文本纠错, Docker, DeepSeek, OpenAI
```

#### 2. 申请 GVP（Gitee 最有价值开源项目）

当项目成熟后（Star 数 > 100，持续维护 3 个月以上），可申请 GVP 认证：
- 访问 https://gitee.com/gvp
- 点击 `申请 GVP`

#### 3. 设置 Gitee Pages（可选）

如果想托管项目文档站点：
1. 在仓库页面点击 `服务` → `Gitee Pages`
2. 选择部署分支（通常是 `main`）
3. 点击 `启动`

---

## 🔄 同时维护 GitHub 和 Gitee

### 配置双远程仓库

```bash
# 查看当前远程仓库
git remote -v

# 应该看到：
# origin    https://github.com/shuke592/TextGuard.git (fetch)
# origin    https://github.com/shuke592/TextGuard.git (push)
# gitee     https://gitee.com/shuke592/TextGuard.git (fetch)
# gitee     https://gitee.com/shuke592/TextGuard.git (push)
```

### 一次性推送到两个平台

创建推送脚本 `push_all.bat`：

```batch
@echo off
echo 正在推送到 GitHub...
git push origin main
if %errorlevel% neq 0 (
    echo GitHub 推送失败！
    pause
    exit /b 1
)

echo 正在推送到 Gitee...
git push gitee main
if %errorlevel% neq 0 (
    echo Gitee 推送失败！
    pause
    exit /b 1
)

echo 推送完成！
pause
```

以后每次更新代码，只需双击 `push_all.bat` 即可同时推送到两个平台。

---

## 📝 日常维护工作流

### 1. 修改代码后提交

```bash
# 查看修改的文件
git status

# 添加修改的文件
git add .

# 提交（使用规范的 commit message）
git commit -m "feat: 添加新功能"
# 或
git commit -m "fix: 修复某个 Bug"
# 或
git commit -m "docs: 更新文档"

# 推送到远程仓库
git push origin main
git push gitee main
```

### 2. Commit Message 规范

采用 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

| 类型 | 说明 | 示例 |
|------|------|------|
| `feat` | 新功能 | `feat: 添加文档批量校对功能` |
| `fix` | Bug 修复 | `fix: 修复文件上传失败的问题` |
| `docs` | 文档更新 | `docs: 更新部署文档` |
| `style` | 代码格式（不影响功能） | `style: 格式化代码` |
| `refactor` | 重构 | `refactor: 重构校对服务` |
| `perf` | 性能优化 | `perf: 优化大文件处理性能` |
| `test` | 测试 | `test: 添加单元测试` |
| `chore` | 构建/工具变更 | `chore: 更新依赖版本` |

### 3. 发布新版本

```bash
# 更新版本号（修改 backend/app/core/config.py 中的 APP_VERSION）
# 提交版本更新
git add .
git commit -m "chore: bump version to v1.1.0"
git push origin main
git push gitee main

# 创建标签
git tag -a v1.1.0 -m "Release v1.1.0"
git push origin v1.1.0
git push gitee v1.1.0

# 在 GitHub/Gitee 上创建 Release（参考前面的步骤）
```

---

## 🎯 推广建议

### 1. 完善项目文档

- ✅ README.md 已完成（包含徽章、功能介绍、快速开始）
- ✅ 添加功能截图到 `docs/images/` 目录
- ✅ 录制演示视频上传到 B站/YouTube，在 README 中添加链接

### 2. 社区推广

- **国内平台**：
  - 掘金：发布技术文章介绍项目
  - CSDN：发布使用教程
  - 知乎：回答相关问题时推荐项目
  - V2EX：在 `/go/project` 板块发帖
  - SegmentFault：发布项目介绍

- **国际平台**：
  - Reddit: r/opensource, r/Python, r/vuejs
  - Hacker News: https://news.ycombinator.com/submit
  - Product Hunt: https://www.producthunt.com/

### 3. SEO 优化

在 README.md 中添加关键词：

```markdown
**关键词**: AI文档校对、智能润色、NLP、文本纠错、开源校对工具、
私有化部署、FastAPI、Vue3、DeepSeek、OpenAI、企业级文档审校
```

### 4. 持续更新

- 每周至少 1 次代码提交（保持项目活跃度）
- 及时回复 Issue 和 Pull Request
- 定期发布新版本（建议每月 1 次小版本更新）

---

## ❓ 常见问题

### Q1: 推送时提示 "Permission denied"

**解决方法**：
```bash
# 使用 HTTPS 方式，输入 GitHub Token 作为密码
# 或配置 SSH 密钥（参考前面的 SSH 配置步骤）
```

### Q2: 不小心提交了敏感文件怎么办？

**解决方法**：
```bash
# 从 Git 历史中彻底删除文件
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch backend/.env" \
  --prune-empty --tag-name-filter cat -- --all

# 强制推送
git push origin --force --all
```

### Q3: 如何删除远程仓库中的文件但保留本地文件？

**解决方法**：
```bash
# 从 Git 跟踪中移除但保留本地文件
git rm --cached <文件路径>
git commit -m "chore: remove sensitive file from git"
git push
```

### Q4: GitHub 访问慢怎么办？

**解决方法**：
- 使用 Gitee 作为主仓库（国内访问快）
- 配置 GitHub 镜像加速：修改 `C:\Windows\System32\drivers\etc\hosts`
  ```
  140.82.113.4 github.com
  199.232.69.194 github.global.ssl.fastly.net
  ```

---

## 📞 获取帮助

如果在发布过程中遇到问题：

1. **查看 Git 官方文档**: https://git-scm.com/doc
2. **GitHub 帮助文档**: https://docs.github.com/
3. **Gitee 帮助中心**: https://gitee.com/help
4. **在项目 Issue 中提问**: 我会及时回复

---

## ✅ 发布检查表

发布前最后确认：

- [x] `.gitignore` 已生效，敏感文件未提交
- [x] README.md 仓库地址已确认
- [x] LICENSE 文件存在（MIT）
- [x] 所有文档中的企业信息已清理
- [ ] 代码可以正常运行（本地测试通过）
- [x] 已创建 GitHub/Gitee 仓库
- [ ] 代码已成功推送
- [ ] 仓库描述、标签已设置
- [ ] 已创建第一个 Release 版本

---

**恭喜你完成第一个开源项目的发布！🎉**

记得在 README.md 中添加你的联系方式，方便社区用户反馈问题和贡献代码。
